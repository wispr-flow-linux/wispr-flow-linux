# Handover — wispr-flow-linux PR triage, 2026-09-22 (end of day)

Paste this into a fresh session in `/home/aaddrick/source/wispr-flow-linux`.

---

You are continuing the September 2026 PR triage for wispr-flow-linux. Read
these first, in order:

1. `CLAUDE.md`
2. `.claude/memory/pr-triage-plan-2026-09.md` (the plan; Phases 0, 1, 3 and
   4 are done, Phase 2 is in PR #84 awaiting the maintainer, Phases 5 and 6
   are open)
3. `docs/learnings/patching-minified-js.md` and
   `docs/learnings/test-methodology.md` (review comments cite sections of
   these rather than re-argue)
4. `.claude/memory/wispr-1.6.897-audit.md` (Phase 1 results)
5. `docs/decisions.md` D-010 (why the installer is pinned, not resolved)

## What is already done

- `main` is at `9bc4e14` (#87, `ACKNOWLEDGMENTS.md`). Landed today on top
  of `f711dbc` (#86), in order: #39 `528b096`, #37 `608a3c7`, #42 `30bf36a`,
  #51 `77f1bbb`, #40 `4ad74f3`, #66 `23c53b5`, #87 `9bc4e14`. Each fork PR
  had `origin/main` merged into it first (pushed to the contributor's branch
  under maintainer edits), then was squash-merged with a written body and a
  `Co-authored-by: Claude <claude@anthropic.com>` line where the merge or an
  edit was mine. Every bundle patch was run twice on the pristine 1.6.897
  bytes (byte-identical second pass, `node --check` clean) before merging.
- Closed with credit: #45 and #74 (to #39), #77 (with the #73 ask), #59
  (yesterday, to #84). Issues #36, #56, #72 closed by #39.
- Round-trip comments posted: #73 (the diagnostic ask; both of its
  remaining anchors are 0 sites on 1.6.897, the force-passthrough one broke
  on adjacency; 1.6.897 drops `{forward:!0}` from the status window's
  ready-to-show `setIgnoreMouseEvents(!0)`), #69 (bats, changelog,
  troubleshooting, doctor line, GNOME no-tray check, rebase), #68 (rework as
  a main-bundle patch on the `ShowAutoPolishPicker` / `ShowFetchLinkPicker`
  forwarding handlers, adding `statusWindow.getBounds()`), #78 (hold), #82
  (new PR since the plan: anchor is dead; asked to widen the win32-gated
  "auto launch at login is enabled" branch in 1.6.897's launch gate, which
  also has a `--show-hub-at-launch` argv flag, and to drop its #73/#74/#77
  stack).
- **PR #84 (`feature/83-pin-installer`, fixes #83) is open and unmerged**,
  now with `origin/main` merged in (`8b6ca3d`, clean). It adds
  `scripts/setup/installer-pin.sh` (1.6.897 + URL + sha256),
  `write-installer-pin.sh`, rewrites `download.sh` to fetch by pin and fail
  closed, makes `extract_installer` refuse a wrong-version `extract/` tree,
  rewires `check-wispr-version.yml` to `latest.json` → pin → tag, points the
  CI build workflows and `test-flags.yml` at the pin, carries #55's resolver
  and appdata commits with @khamsakamal48's authorship, adds
  `tests/installer-pin.bats` (41 tests), D-010, and docs.
- The Nix helper pin is v0.1.2 with a real hash. `nix` is not on this host;
  the hash came from a Python NAR serialiser (in the session scratchpad,
  not committed) that reproduces the v0.1.0 hash nix reported. If the pin
  moves again and the routine is gone, re-derive: extract the GitHub
  tarball with `--strip-components=1`, NAR-serialise (`nix-archive-1`,
  sorted entries, executable bit, no mtimes), sha256, base64 with the
  `sha256-` prefix.
- CI is approved on every open fork PR; future pushes from first-time
  contributors need `gh run list --status action_required` then
  `gh api -X POST repos/wispr-flow-linux/wispr-flow-linux/actions/runs/<id>/approve`.
- `xorg-x11-server-Xvfb` is installed, so the AppImage artifact test's
  headless launch runs locally.

## Still true, and load-bearing

- **Merging #84 re-arms the nightly bump bot**, and that is now the
  intended outcome (see next action 1). `WISPR_FLOW_VERSION` is still
  `1.6.7`; the first run after merge finds the pin at 1.6.897, sets the
  variable, and pushes `v1.0.3+wispr1.6.897`.
- The verify-patches marker list is now 11 fixed strings + 1 Perl regex
  (`hub-focusable`, `early-singleton`, `disable-pill-drag` added today).
  Every new marker needs its `MARKER_SAMPLES` twin in
  `tests/verify-patches.bats` or the omit-one matrix goes red (#73 and #77
  failed CI exactly this way).
- `linux-disable-pill-drag.sh` anchors on the handler's `let t,n;if(`
  prelude, which 1.5.789 did not have. It matches one site on 1.6.7 and
  1.6.897 and fails closed; loosen it per the adjacency section when next
  touched, with a near-miss fixture that has the `if(` but no `let`.
- `scripts/build-linux.sh` step 2 does `rm -rf "$WORK_DIR"` (all of
  `build-linux/`), which deletes `build-linux/downloads/` every build. Keep
  a local `--exe` outside `build-linux/`. Phase 5 item; not filed yet.
- `extract/` holds 1.6.897 and is version-checked on reuse; `extract-1.5.789/`
  (untracked, not gitignored) holds 1.5.789 for comparisons. Pristine
  extracted asars for both live only in the session scratchpad
  (`npx @electron/asar extract extract/nupkg/lib/net45/resources/app.asar <dir>`
  recreates them in seconds).

## Open PRs after today (10)

| PR | State | Next step |
|---|---|---|
| #84 | maintainer's call | gate decision, then squash-merge + dispatch |
| #55 | one commit untaken | `1b2238c` (Windows shortcut map on Linux, fixes #33 #46) has a one-letter typo at `tests/verify-patches.bats:40` that fails codespell, and has not been re-checked against 1.6.897 (eight `win?:mac` ternary reads in the shortcuts module, fails closed if any is not a ternary). Cherry-pick with authorship after that check, or ask the author. |
| #70 | redirected 09-21 | RELEASES + nupkg fallback verified by the manifest sha1; waiting on author |
| #69 | round-trip posted | see plan |
| #68 | round-trip posted | main-side rework; #78 rebases off it after |
| #78 | hold | helper #19/#20 review + v0.1.3 (Phase 6) |
| #73 | diagnostic ask posted | hotplug logs + `xwininfo -shape`; drop two of three patches |
| #82 | round-trip posted | widen the 1.6.897 launch-gate branch instead |
| #67 | untouched | plan says drop; closes with the Phase 6 decision |

## Next actions, in order

1. **Land #84 and let the bot run.** The maintainer decided on 2026-09-22:
   no publish gate, automatic and fail-closed (details under Phase 2 in the
   plan). Squash-merge #84 when they say go (they may do it themselves),
   then `gh workflow run check-wispr-version.yml`. Expected: the run finds
   the pin already at 1.6.897, sets `WISPR_FLOW_VERSION`, pushes
   `v1.0.3+wispr1.6.897`, and `ci.yml` runs build → test → release → APT →
   DNF → AUR. Watch for: sha256 check passes, `verify-patches` prints eleven
   OK lines, the smoke assert on the headless runner reads `injection:
   X11`, the Release has all assets, and the next `apt-repo-heartbeat` is
   green. If a job goes red, nothing ships: fix on main and re-tag with a
   `+rebuild.N` suffix rather than moving the tag.
2. **rc-tag exclusion PR**, right after the first bump lands: add
   `!contains(github.ref_name, '-rc')` to the three publish jobs and
   `prerelease:` for rc tags in `release`, plus a RELEASING.md paragraph
   saying an rc tag is the manual look-first path. Add D-011 to
   `docs/decisions.md` in the same PR.
3. **#55's last commit**: fix the typo, re-check the shortcuts anchor on
   1.6.897, cherry-pick with `--author` preserved (or ask @khamsakamal48),
   then close #55 with credit. Add a line to `ACKNOWLEDGMENTS.md`.
4. **Phase 5** as written in the plan, each as its own PR after the first
   clean auto-bump. Add: the `build-linux/` wipe footgun, a gate audit of
   the 13 new `"win32"===process.platform` reads listed in
   `wispr-1.6.897-audit.md` (the "auto launch at login is enabled" hub gate
   is one of them and is what #82 should widen), and the `let t,n;`
   loosening for `linux-disable-pill-drag.sh`.
5. **Phase 6** needs the maintainer's pill-strategy decision first. The
   `WISPR_USE_X11=1` launcher opt-in needs no decision and can go first.
6. Answer round-trips as authors respond; approve CI on their pushes.

## Conventions to keep

- Branch names `fix/<issue>-desc` or `feature/<issue>-desc`; squash-merge
  with `(#N)` in the subject and a written body (what, why, what was
  verified, `Fixes #N`, co-author lines); `Co-Authored-By: Claude
  <claude@anthropic.com>` trailer on commits. Issues and PRs are short and
  plain (CLAUDE.md, "Issues, PRs, and commits"). Comments carry no trailer.
- To land a fork PR: `git fetch origin pull/N/head:pr-N`, work in a
  `git worktree` under the scratchpad, `git merge origin/main`, edit, run
  `bats tests/*.bats`, `shellcheck`, `codespell`, `actionlint`, run each
  new patch twice on the pristine bundle, then
  `git push https://github.com/<owner>/wispr-flow-linux.git HEAD:<branch>`
  and `gh pr merge N --squash --subject ... --body ...`. Resolve conflicts
  with line-anchored markers (`^<<<<<<< `, `^=======$`); `git checkout
  --merge` relabels them `ours`/`theirs`, and a `# ====` comment line ends
  in `=======`.
- Bash: tabs, 80 cols, `[[ ]]`, no `set -e` (except the existing
  `scripts/patches/` files, which use `set -euo pipefail` and 2-space indent
  as house style there; `scripts/build-linux.sh` is 2-space too).
- `grep` is shadowed by a ugrep wrapper in this shell; use `/usr/bin/grep`
  when a byte-count or `-o` result matters. `$TMPDIR` is unset; use the
  session scratchpad path.
- Any desktop smoke launch must use an isolated `HOME`/`XDG_*` so the
  maintainer's live profile is never migrated, and must say so first.
- `build.log` in the repo root is untracked scratch; leave it alone.

## Open decisions for the maintainer (do not decide these yourself)

- The Wayland pill strategy: GNOME-extension-only input shaping (review the
  five stacked helper PRs #16–#20 and cut v0.1.3) versus a `WISPR_USE_X11=1`
  launcher opt-in as the cross-compositor fallback. #78, #68, #67 and the
  reworked #73 all key off this. Recommendation on record (2026-09-22): do
  both, X11 opt-in first, and treat #78's title channel as
  compositor-neutral with the GNOME extension as its first consumer and a
  KWin script as the second.

## Decided

- Publish gate: none. Automatic and fail-closed, like claude-desktop-debian
  (2026-09-22). See next action 1 and the plan's Phase 2 note.
