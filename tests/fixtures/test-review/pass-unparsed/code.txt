_doctor_check_disk() {
	local avail
	avail=$(df --output=avail -BM "$HOME" | tail -1 | tr -d ' M')
	if [[ -z $avail ]]; then
		_warn 'could not read free space'
		return
	fi
	if (( avail < 500 )); then
		_fail "only ${avail}M free"
		return
	fi
	_pass "free space: ${avail}M"
}
