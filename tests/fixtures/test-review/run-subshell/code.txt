_doctor_failures=0
_fail() {
	echo "[FAIL] $*"
	_doctor_failures=$((_doctor_failures + 1))
}
_doctor_check_clipboard() {
	if ! command -v wl-copy >/dev/null 2>&1; then
		_fail 'wl-copy not found'
		return
	fi
	echo '[PASS] wl-copy found'
}
